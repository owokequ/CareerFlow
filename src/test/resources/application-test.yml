spring:
  kafka:
    bootstrap-servers: localhost:9092
    admin:
      auto-create: false
    listener:
      auto-startup: false

  jpa:
    open-in-view: false

  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secretAccess: test-access-secret-must-be-at-least-32-bytes-long
  secretRefresh: test-refresh-secret-must-be-at-least-32-bytes-long

app:
  mail:
    from: test@owoke.local
